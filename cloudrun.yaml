apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: finance-agent-api
spec:
  template:
    spec:
      containers:
        - image: REGION-docker.pkg.dev/PROJECT/REPO/finance-agent-api:latest
          ports:
            - containerPort: 8080
          env:
            - name: NODE_ENV
              value: production
